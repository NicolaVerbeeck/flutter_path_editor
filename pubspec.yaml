name: path_editor
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.4.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_parsing: ^1.1.0
  meta: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
